// the .includes();
// works on strings and arrays
"Hello".includes("o");
// returns boolean

const pets = ["cat", "dog", "bat"];

pets.includes("birds");

// exponential operator **

const square = (x) => x**2;
const cube = (y) => y**3;